[build]
  command = "npm install --retry 3 && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18.18.2"
  # Disable unnecessary language installations
  GO_VERSION = ""
  PYTHON_VERSION = ""
  RUBY_VERSION = ""
