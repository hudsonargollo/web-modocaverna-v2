[build]
  command = "npm install --legacy-peer-deps && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18.18.2"
  NPM_FLAGS = "--legacy-peer-deps"
  
[context.production.environment]
  NEXT_PUBLIC_ENV = "production"
  NEXT_PUBLIC_GA_ID = "UA-XXXXXX-1"  # Your actual GA ID
