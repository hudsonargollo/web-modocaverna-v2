[build]
  command = "npm install sharp --platform=linux --arch=x64 && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18.18.2"  # Required for Next.js compatibility
  # Remove all Go-related configurations
